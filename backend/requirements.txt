
# torch and torchvision are intentionally NOT listed here — install them
# separately using the CPU-only index to avoid pulling ~10GB of CUDA
# dependencies:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# (The Dockerfile already does this automatically for container builds.)

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
sqlalchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.10
python-jose[cryptography]==3.3.0
bcrypt==4.0.1
python-multipart==0.0.9
slowapi==0.1.9
redis==5.0.8
Pillow==10.4.0
python-magic-bin==0.4.14; sys_platform == "win32"
python-magic==0.4.27; sys_platform != "win32"
openai==1.51.0
httpx==0.27.2
pytest==8.3.3
pytest-asyncio==0.24.0
reportlab==4.2.5
